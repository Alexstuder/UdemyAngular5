import { MouseHooverDirective } from './mouse-hoover.directive';

describe('MouseHooverDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseHooverDirective();
    expect(directive).toBeTruthy();
  });
});
