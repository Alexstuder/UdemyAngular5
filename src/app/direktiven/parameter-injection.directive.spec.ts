import { ParameterInjectionDirective } from './parameter-injection.directive';

describe('ParameterInjectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ParameterInjectionDirective();
    expect(directive).toBeTruthy();
  });
});
